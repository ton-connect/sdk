---
'@tonconnect/ui': patch
---

Ship Telegram and TON brand icons as bundled data URIs instead of loading them from raw.githubusercontent.com, so they render without external requests and under a strict CSP
