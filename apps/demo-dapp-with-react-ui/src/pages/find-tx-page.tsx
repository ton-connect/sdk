import { Layout } from '@/core/components';
import { FindTransactionDemo } from '@/features/utilities';

export const FindTxPage = () => (
    <Layout title="Find transaction">
        <FindTransactionDemo />
    </Layout>
);
