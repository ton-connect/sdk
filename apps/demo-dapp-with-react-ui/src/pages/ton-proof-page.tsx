import { Layout } from '@/core/components';
import { TonProofDemo } from '@/features/signing';

export const TonProofPage = () => (
    <Layout title="Ton proof">
        <TonProofDemo />
    </Layout>
);
