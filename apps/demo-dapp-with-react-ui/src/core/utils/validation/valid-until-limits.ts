/** Recommended max lifetime for sendTransaction / signMessage `validUntil` (seconds from now). */
export const TRANSACTION_VALID_UNTIL_MAX_SECONDS = 48 * 60 * 60;

export const TRANSACTION_VALID_UNTIL_MAX_HOURS = TRANSACTION_VALID_UNTIL_MAX_SECONDS / 3600;
