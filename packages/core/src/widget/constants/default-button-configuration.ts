import { ButtonConfiguration } from 'src/widget/models/button-configuration';

export const defaultButtonConfiguration: Required<ButtonConfiguration> = {
    theme: 'DARK',
    size: 'm',
    appearance: 'primary'
};
